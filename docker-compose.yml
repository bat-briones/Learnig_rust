version: "3.9"

services:
  rust:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./:/workspace
    working_dir: /workspace
    tty: true
    stdin_open: true
    command: cargo watch -x run
