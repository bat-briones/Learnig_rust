fn main() {
    /*
    types of variables:
    scalar types: rust has 4 types of scalar types: integer,
    floating-point, boolean and character
    this types are the tradicional types of variables in programming languages
      */

    //integers types
    // rust has different types of integgers, with sign and without sign, and with different sizes
    // the size of the integer is in bits
    // without sign we can save 2^(n-1) (n is the value of the size in bits)
    // with sign we can save 2^(n-1) - 1 (n is the value of the size in bits)
}
